# Sanitized coexistence record

Existing root authorities remain in force: `AGENTS.md` and `CLAUDE.md`.

Pending work remains authoritative at `PENDING.md`.

Narrative decisions remain authoritative at `DECISIONS.md`.

The existing role remains at `.claude/roles/builder.md`.

The learning reference remains inactive at `.claude/knowledge/testing.md`.

Generated `.claude/worktrees/generated-context.md` is excluded from context and ownership.

Secret-bearing `.env.example` is inventoried by identity only; values are never migrated.

Verification provenance points to `tests/verification-source.txt`.

No original may be deleted without semantic-equivalence review and separate cutover authorization.
