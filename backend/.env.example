DB_USERNAME=postgres
DB_PASSWORD=troque-por-sua-senha-local

# Chave usada para assinar os JWTs. Gere uma string aleatória longa (>= 32 bytes),
# por exemplo: openssl rand -base64 32
JWT_SECRET=troque-por-uma-chave-secreta-longa-e-aleatoria
JWT_EXPIRATION_MINUTES=60
